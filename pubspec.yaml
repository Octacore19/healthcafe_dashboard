name: healthcafe_dashboard
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  firebase_core: ^2.13.1
  google_fonts: ^4.0.4
  equatable: ^2.0.5
  firebase_auth: ^4.6.3
  shared_preferences: ^2.1.2
  firebase_storage: ^11.2.3
  dio: ^5.0.0
  cloud_firestore: ^4.3.1
  flutter_screenutil: ^5.6.0
  flutter_svg: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/
    - assets/gifs/
    - assets/poppins_fonts/
    - assets/inter/